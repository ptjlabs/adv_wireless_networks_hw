# This file will serve as a place to test graphs

import matplotlib.pyplot as plt
import numpy as np
import math

# x = np.arange(-8,8,0.1)
# f = 1 / (1 + np.exp(-x))

# plt.plot(x,f)
# plt.xlabel('x')
# plt.ylabel('f(x)')
# plt.show()

# def norm_log_util_function(rate,rate_of_utilization):
#     container = []
#     for ri in rate:
#         math.log(1 + (rate_of_utilization * ri))/(math.log(1 + (rate_of_utilization * max(rate))))

#     return container

# f = norm_log_util_function([x for x in range(50)],15)
# print(f)
result = (5) * (5)
print(result)